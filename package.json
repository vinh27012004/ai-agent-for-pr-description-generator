{
  "name": "pr-description-agent",
  "version": "1.0.0",
  "description": "AI Agent tạo PR description từ git diff — qua 9router (OpenAI-compatible) + GitHub webhook",
  "type": "module",
  "scripts": {
    "demo": "node scripts/generate-pr.js --demo",
    "generate": "node scripts/generate-pr.js",
    "server": "node src/server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "openai": "^4.77.0"
  }
}
